<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="to_post.list">
      <field name="name">to_post list</field>
      <field name="model">to_post.to_post</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="to_post.action_window">
      <field name="name">to_post window</field>
      <field name="res_model">to_post.to_post</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="to_post.action_server">
      <field name="name">to_post server</field>
      <field name="model_id" ref="model_to_post_to_post"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="to_post" id="to_post.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="to_post.menu_1" parent="to_post.menu_root"/>
    <menuitem name="Menu 2" id="to_post.menu_2" parent="to_post.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="to_post.menu_1_list" parent="to_post.menu_1"
              action="to_post.action_window"/>
    <menuitem name="Server to list" id="to_post" parent="to_post.menu_2"
              action="to_post.action_server"/>
    -->
  </data>
</odoo>